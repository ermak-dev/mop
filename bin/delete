#!/usr/bin/env python3
"""удалить папета: mop delete <имя>

Клон на узле остаётся лежать и переиспользуется, если папета поднять снова.
"""
import lib
from mop import nomad, puppets


def main(argv):
    if len(argv) != 1:
        lib.usage(__doc__)
    name = argv[0]
    lib.guard(name)
    nomad.deregister(name)
    print(f"удалён {name} (клон остался в {puppets.clone_dir(name)} на узле)")


lib.run(lib.cluster(main))
