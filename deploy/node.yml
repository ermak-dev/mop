---
# Чем узел обязан быть прежде всего остального. Слой идёт ПЕРВЫМ в site.yml: и
# окружение узлов, и шина уже пользуются этим набором, а не доставляют его.
#
# Run:  mop deploy

- name: Base prerequisites of a pool node
  hosts: puppet
  become: no
  gather_facts: no
  roles:
    - node
